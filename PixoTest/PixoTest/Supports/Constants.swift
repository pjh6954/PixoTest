//
//  Constants.swift
//  PixoTest
//
//  Created by Dannian Park on 2021/03/08.
//

import Foundation
import UIKit
struct constants {
    public static let tableCellHeight : CGFloat = 100
    //Margin, Padding 정의
    public static let defaultMarginPadding : CGFloat = 10
    /*
    struct margin {
        //default
        static let (left, right, top, bottom) : (Int, Int, Int, Int) = (defaultMarginPadding, defaultMarginPadding, defaultMarginPadding, defaultMarginPadding)
        
        //special
    }
    
    struct padding {
        static let (left, right, top, bottom) : (Int, Int, Int, Int) = (defaultMarginPadding, defaultMarginPadding, defaultMarginPadding, defaultMarginPadding)
        
        //special
    }
    */
}
